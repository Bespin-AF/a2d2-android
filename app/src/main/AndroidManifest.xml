<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.bespinaf.a2d2">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controllers.RideRequestDetails"></activity>
        <activity
            android:name=".controllers.RideRequests"
            android:label="Ride Requests" />
        <activity android:name=".controllers.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".controllers.Rules"
            android:label="@string/a2d2_rules_title"
            android:parentActivityName=".controllers.MainActivity" />
        <activity
            android:name=".controllers.RequestRide"
            android:label="Pickup Request Options"
            android:parentActivityName=".controllers.Rules"
            android:windowSoftInputMode="stateHidden|adjustResize" />
        <activity
            android:name=".controllers.RideStatus"
            android:label="Ride Status"
            android:parentActivityName=".controllers.RequestRide" />
        <activity
            android:name=".controllers.DriverLogin"
            android:label="Driver Login"
            android:parentActivityName=".controllers.MainActivity" />
    </application>

</manifest>